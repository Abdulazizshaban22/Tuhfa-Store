global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.rules.yml

scrape_configs:
  - job_name: 'tohfa-api'
    metrics_path: /api/metrics
    static_configs:
      - targets: ['api:3001']
